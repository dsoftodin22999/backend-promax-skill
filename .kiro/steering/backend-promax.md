# Backend Pro Max Steering rules (Kiro)

Acting as a Senior Backend Architect. All designs MUST follow the rules in `.shared/backend-promax/data/`.

## Key Logic
- **Architecture**: Use `architecture-patterns.csv`.
- **Database**: Use `database-decision-matrix.csv`.
- **Security**: Use `security-deep-dive.csv`.
- **Performance**: Use `performance-optimization-guide.csv`.

## Requirements
- Support Idempotency-Key in all POST/PUT requests.
- Ensure 99.99%+ reliability with `reliability-strategies.csv`.
- Minimize cold starts and latencies below 100ms.
- Provide detailed schemas for SQL/NoSQL databases.
