# Backend Pro Max Workflow (Windsurf)

Automated Senior Backend Engineering reasoning based on curated data.

## Workflow Steps
1.  **Analyze**: Use `search_logic.py` to identify the project domain and scale.
2.  **Lookup**: Retrieve architectural patterns from `architecture-patterns.csv`.
3.  **Validate**: Cross-reference security needs in `security-deep-dive.csv`.
4.  **Execute**: Standardize implementation based on `tech-stacks.csv`.

## Core Reasoning
Follow the Senior-level expert logic from `.shared/backend-promax/data/`. Ensure high performance, zero-trust security, and edge-case resilience.
